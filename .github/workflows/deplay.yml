name: Deploy porteFolio-V2

on:
  branches:
    - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Install
        run: npm ci
      
      - name: build
        run: npm run build
      
      
      
